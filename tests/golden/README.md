# Golden fixtures

This directory contains minimal, platform-stable fixtures used by the test suite.

The golden roots represent the expected payload Merkle root hash for specific
reference run configurations.
