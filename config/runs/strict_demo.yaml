schema_version: "1.0"
run:
  seed: 1337
  run_name: "strict_demo"
  hermetic: true
  output_root: "artifacts/runs"
inputs:
  trade_feed:
    records: 200
  registry:
    entities: 40
rules:
  pack: "strict"
  paths: []
