schema_version: "1.0"
run:
  seed: 2024
  run_name: "adversarial"
  hermetic: true
  output_root: "artifacts/runs"
inputs:
  trade_feed:
    records: 80
  registry:
    entities: 20
rules:
  pack: "baseline"
  paths:
    - "contracts/quality_rules/quality_rules.yaml"
