schema_version: "1.0"
run:
  seed: 7
  run_name: "backfill"
  hermetic: true
  output_root: "artifacts/runs"
inputs:
  trade_feed:
    records: 60
  registry:
    entities: 18
rules:
  pack: "baseline"
  paths:
    - "contracts/quality_rules/quality_rules.yaml"
