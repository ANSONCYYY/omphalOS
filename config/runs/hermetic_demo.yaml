schema_version: "1.0"
run:
  seed: 42
  run_name: "hermetic"
  hermetic: true
  output_root: "artifacts/runs"
inputs:
  trade_feed:
    records: 120
  registry:
    entities: 25
rules:
  pack: "baseline"
  paths:
    - "contracts/quality_rules/quality_rules.yaml"
