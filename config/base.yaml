schema_version: "1.0"
run:
  seed: 1337
  run_name: "reference"
  hermetic: true
  output_root: "artifacts/runs"
inputs:
  trade_feed:
    records: 200
  registry:
    entities: 40
rules:
  pack: "baseline"
  paths:
    - "contracts/quality_rules/quality_rules.yaml"
