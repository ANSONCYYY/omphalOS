name: omphalos_warehouse
version: "1.0"
config-version: 2

profile: omphalos_duckdb

model-paths: ["models"]
macro-paths: ["macros"]
test-paths: ["tests"]
seed-paths: ["seeds"]

models:
  omphalos_warehouse:
    +materialized: view
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
