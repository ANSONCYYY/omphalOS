# Rerun

To rerun deterministically, reuse the same run config and inputs.

```bash
omphalos run --config <config.yaml>
```

Compare two runs:

```bash
omphalos certify --run-a <runA> --run-b <runB>
```
