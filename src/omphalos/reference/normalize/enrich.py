from __future__ import annotations

from typing import Any, List


def enrich_trade_feed(rows: List[dict[str, Any]]) -> List[dict[str, Any]]:
    # Placeholder for feature enrichment (kept deterministic).
    return list(rows)
