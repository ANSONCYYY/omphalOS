from __future__ import annotations

ASSUMPTIONS = {
    "scoring": "Chokepoint score blends concentration in illustrative categories with magnitude normalization.",
    "resolution": "Entity matching is token-based, deterministic, and favors auditability.",
}
